services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    image: rss-reader:local
    container_name: rss-reader
    restart: unless-stopped
    ports:
      - "127.0.0.1:9898:8080"
    volumes:
      - ./config.json:/app/config.json
      - ./archives.txt:/app/archives.txt
